{
  "name": "@openludus/dashboard-server",
  "version": "0.1.0",
  "description": "Zero-dependency Node.js HTTP + SSE server that ingests hook events and serves the dashboard UI",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
